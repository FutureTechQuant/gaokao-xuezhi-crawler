name: xuezhi-major-list

on:
  push:
    branches: [ main ]
    paths:
      - 'src/sources/xuezhi/major_list_spider.py'
      - 'src/pipelines/stage_01_collect_list_pages.py'
      - 'src/common/**'
      - '.github/workflows/xuezhi-major-list.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/sources/xuezhi/major_list_spider.py'
      - 'src/pipelines/stage_01_collect_list_pages.py'
      - 'src/common/**'
      - '.github/workflows/xuezhi-major-list.yml'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      TARGET: xuezhi_major
      MAX_PAGES: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install chromium
      - name: Run stage 01
        run: python -m src.pipelines.stage_01_collect_list_pages --target ${TARGET}
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xuezhi-major-stage01
          path: |
            data/raw/xuezhi/major/list_pages/**
            data/stage/01_list_index/**
